import { Button } from "@/components/ui/button";

const Playground = () => {
  const test = async () => {};

  return (
    <div>
      <Button onClick={test}>Click Me</Button>
    </div>
  );
};

export default Playground;
